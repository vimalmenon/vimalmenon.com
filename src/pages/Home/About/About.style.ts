import styled from 'styled-components';

export const AboutContainerStyled = styled('section')(() => ({
  display: 'flex',
}));

export const AboutImageStyled = styled('section')(() => ({
  display: 'flex',
  flex: '1',
}));

export const AboutDetailStyled = styled('section')(() => ({
  display: 'flex',
  flex: '1',
}));
